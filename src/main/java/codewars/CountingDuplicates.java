package codewars;

import java.util.HashMap;
import java.util.Map;

public class CountingDuplicates {
/*
    public static int duplicateCount(String text) {
        text = text.toLowerCase();   // Convert the character to lowercase

        Map<Character, Integer> charCounts = new HashMap<>(); // Make a map to store the character counts


        for (char c : text.toCharArray()) {       // Iterate through the characters in the string


            // If the character is already in the map, increment its count
            if (charCounts.containsKey(c)) {
                charCounts.put(c, charCounts.get(c) + 1);
            }
            // Otherwise, add it to the map with a count of 1
            else {
                charCounts.put(c, 1);
            }
        }

        // Initialize a count for the number of duplicates
        int duplicateCount = 0;

        // Iterate through the character counts
        for (int count : charzadanieCounts.values()) {
            // If the count is greater than 1, increment the duplicate count
            if (count > 1) {
                duplicateCount++;
            }
        }

        // Return the count of duplicates
        return duplicateCount;
    }

*/
}
